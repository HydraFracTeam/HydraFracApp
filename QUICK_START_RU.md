# HydraFracApp: Краткая шпаргалка (Quick Start)

## АЛГОРИТМ В 5 ШАГОВ

```
1. Загружаете CSV (t, P, Q)
    ↓
2. Вводите параметры пласта (h, φ, μ, Ct, P0, N)
    ↓
3. Устанавливаете границы оптимизации (L, k)
    ↓
4. Запускаете solver
    ↓
5. Получаете результат: S, k, L, a/L, N
```

---

## ТИПИЧНАЯ СЕССИЯ (5 МИНУТ)

### Шаг 1: Загрузить данные
```
[Загрузить файл] → выбрать CSV → OK
→ Autosplit: выбрать "Только спад" → OK
```

### Шаг 2: Статические параметры
| Поле | Типичное значение | Источник |
|------|-------------------|----------|
| h | 10–50 м | геология скважины |
| φ | 0.15–0.25 | керны, ГК |
| μ | 1–3 сП | PVT анализ |
| $B_o$ | 0.9–1.1 | PVT анализ |
| $C_t$ | 1–5×10⁻⁵ 1/psi | осредненное |
| P0 | начальное давление | манометр |
| N | кол-во трещин | из МГРП плана |
| Q | дебит | если нет в CSV |

**Совет:** Если N неизвестно → оставьте 0 или 1, solver подберет

### Шаг 3: Границы оптимизации

**Правило:** ±10× от геологической оценки

```python
L_геол = 50  # м (ожидаемое из геологии)
L_min = max(1, L_геол / 10)     # = 5 м
L_max = min(500, L_геол * 10)   # = 500 м

k_геол = 0.1  # мД
k_min = max(0.00001, k_геол / 100)  # = 0.001 мД
k_max = min(10, k_геол * 100)       # = 10 мД
```

### Шаг 4: Нажать "Рассчитать"

Solver выполняет:
- Шаг 1: подбор Skin (из 50+ дискретных значений)
- Шаг 2: подбор N (если не зафиксирован)
- Шаг 3: подбор a/L (из 20+ дискретных)
- Шаг 4: подбор L (интерполяция между соседями)
- Шаг 5: восстановление k из масштаба X

Время: 10–60 сек в зависимости от размера данных

### Шаг 5: Интерпретировать результат

```
✅ Если misfit < 0.01 и R² > 0.99:
   → результат хороший, используйте его
   
⚠️ Если misfit > 0.1 или R² < 0.95:
   → примените сглаживание и перезапустите
   
❌ Если L ≈ L_min или L ≈ L_max:
   → расширьте границы и перезапустите
```

---

## ТИПИЧНЫЕ ОШИБКИ

### ❌ "Недостаточно данных для расчёта"
**Причина:** < 10 точек с Y > 0  
**Решение:** увеличьте φ, h или уменьшите L_max в границах

### ❌ Очень маленькое k (< 0.001 мД)
**Причина:** вероятно, h слишком большая  
**Решение:** пересчитайте h из керна, а не из предположения

### ❌ Высокая невязка (misfit > 0.01)
**Причина:** данные содержат шум  
**Решение:** примените фильтр сглаживания перед расчетом

### ❌ L = L_max
**Причина:** истинное L вне границ  
**Решение:** увеличьте L_max до 500 м

---

## ОЖИДАЕМЫЕ ПОГРЕШНОСТИ

### На идеальных синтетических данных (1000 тестов):
```
S   : RMSE = 0.000     (идеально)
N   : RMSE = 0.000     (идеально, если дискретно)
a/L : RMSE = 0.000     (идеально)
L   : RMSE = 0.016 м   (±0.02 м, практически идеально)
k   : RMSE ≈ 0.015 мД  (±0.5% MAPE)
R²  : 0.99913          (совпадение форм кривых)
```

### На реальных данных с 5% шумом (оценка):
```
L   : RMSE ≈ 0.5–2 м   (зависит от L)
k   : RMSE ≈ ±20–30%
```

### На реальных данных с 10% шумом:
```
L   : RMSE ≈ 2–5 м
k   : RMSE ≈ ±50%
```

**Вывод:** если σ(P) > 5%, применяйте сглаживание!

---

## ПРОВЕРКА РЕЗУЛЬТАТА

```
✅ L разумное? 
   → сравнить с геологией и расстоянием МГРП

✅ k согласуется с типом коллектора?
   → песчаник 0.01–1 мД, карбонат 0.001–0.1 мД

✅ N соответствует плану МГРП?
   → часто недооценивается (coalescence)

✅ S ∈ (−2, +3)?
   → вне этого диапазона → пересчитайте

✅ misfit < 0.01?
   → иначе данные не интерпретированы
```

---

## СЛОЖНЫЕ СЛУЧАИ

### Если данные плохие (σ > 10%)
```
1. [Фильтр] → гауссово сглаживание
2. [Удаление выбросов] 
3. [Интерполяция] (если есть пропуски)
4. Перезапустить solver
```

### Если результат не сходится
```
1. Пересчитать статические параметры (особенно φ, h)
2. Расширить границы L на ±20%
3. Проверить Q (постоянный ли он, или меняется?)
4. Если все равно → данные не подходят для модели
```

### Если много трещин (N > 3)
```
1. Проверить, правильно ли задано N в МГРП плане
2. Возможна коалесценция (слиние) трещин
3. Рассмотреть N как неизвестное (подбирается)
```

---

## ФОРМУЛЫ (ДЛЯ СПРАВКИ)

$$X = \frac{0.00864 \cdot k \cdot h \cdot |\Delta P|}{\mu \cdot B_o \cdot Q}$$

$$Y = \frac{Q \cdot B_o \cdot t}{24 \cdot \phi \cdot C_t \cdot h \cdot |\Delta P| \cdot L^2}$$

$$k_{real} = k_{ref} \cdot \frac{median(X_{fact})}{median(X_{ref})}$$

где k_ref = 5.0 мД (стандартное значение библиотеки)

---

## ГОРЯЧИЕ КЛАВИШИ (ЕСЛИ РЕАЛИЗОВАНЫ)

| Команда | Горячая клавиша |
|---------|-----------------|
| Загрузить файл | Ctrl+O |
| Сохранить сессию | Ctrl+S |
| Применить фильтр | Ctrl+F |
| Рассчитать | Ctrl+Enter |

---

## КОНТРОЛЬНЫЙ СПИСОК ПЕРЕД ЗАПУСКОМ

- [ ] Загружены данные KVD (спад) без шума > 5%
- [ ] Все параметры h, φ, μ, Ct, P0 введены
- [ ] N задано (или 0, если подбирается)
- [ ] Границы L, k установлены ±10× от оценки
- [ ] Таблица показывает 20+ валидных точек
- [ ] Граница L_max ≤ 500 м

Если все ✅ → нажмите "Рассчитать"

---

## КОНТАКТЫ И СПРАВКА

- Полная инструкция: `USER_GUIDE_RU.md`
- Результаты тестов: `tests/TEST_REPORT_1000_RUS.md`
- Исходный код солвера: `solver/solver_new.py`

**Версия:** 1.0 (май 2026)
